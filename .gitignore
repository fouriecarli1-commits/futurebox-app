node_modules/
.next/
out/
.env*.local
next-env.d.ts
*.tsbuildinfo

# Browser probes: the scripts and their fixtures. The pages they mount live
# beside the code as *.probe.tsx and are only a page when PROBE=1, so they can
# be re-run — see .probe/README.md, which is kept.
.probe/*
!.probe/README.md
